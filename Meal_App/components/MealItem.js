import { Text, View, Pressable } from "react-native";

function MealItem({ title }) {
  return (
    <View>
      <Pressable></Pressable>
      <Text>{title}</Text>
    </View>
  );
}

export default MealItem;
